class Node:
    def __init__(self, nodes, metadata):
        self.nodes = nodes
        self.metadata = metadata

    def __repr__(self):
        return "Children: %s, metadata: %s" % (len(self.nodes), len(self.metadata))

    def __str__(self):
        return "{}".format(self.metadata)

